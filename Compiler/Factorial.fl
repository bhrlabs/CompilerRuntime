let
	integer n, x, z;
	fun fact(integer a;)
		if a = 1
		then
		return a;
		else
		skip;
		fi;
		z := a - 1;
		x := call fact(z);
		a := a * x;
		return a;
	end_fun
in
	n := 6;
	x := call fact(n);
	write x;
end